groups:
  - name: rul_api_alerts
    rules:
      - alert: HighPredictionLatency
        expr: up == 1
        for: 0s
        labels:
          severity: warning
        annotations:
          summary: "API Monitoring Active & Triggered!"
          description: "Prometheus successfully generated a test alert."