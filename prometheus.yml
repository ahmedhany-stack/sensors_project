global:
  scrape_interval: 2s
rule_files:
  - "alert_rules.yml"  
scrape_configs:
  - job_name: 'fastapi_rul_app'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['localhost:8000']

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - 'localhost:9093' 